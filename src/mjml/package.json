{
  "name": "mjml",
  "version": "1.0.0",
  "scripts": {
    "emails:compile-html": "npx mjml --config.keepComments=0 ./templates/**/templates/*/*.mjml -o ../emails/templates",
    "emails:copy": "./copy.sh",
    "emails": "npm run emails:compile-html && npm run emails:copy"
  },
  "private": true,
  "license": "ISC",
  "dependencies": {
    "mjml": "^4.16.1"
  }
}
