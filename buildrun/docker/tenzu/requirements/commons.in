# channel
channels[daphne,types]
channels_redis
redis[hiredis]

# API
pydantic[email]>2
django-ninja

# Quality of life
django-extensions
pydantic-settings
orjson
uvloop>=0.18.0

# Deployment
a2wsgi # WSGI to ASGI adapter
uvicorn[standard]
uvicorn-worker
gunicorn
granian

# ORM + Django
Django[argon2]
psycopg[c]
easy-thumbnails[svg] # Images and thumbnails
django-nonrelated-inlines # admin
django-cors-headers

# Task Queue Module
procrastinate[django]

# Auth
PyJWT # For auth
cryptography # For auth
django-allauth[socialaccount,openid,saml,headless]
django-auth-ldap

# storage
django-storages[s3]
whitenoise[brotli]

# Utils & tools
faker # For sampledata
python-json-logger # JSON logger
python-slugify # To generate slugs
sentry-sdk  # error event handler

#pycrdt
pycrdt-websocket

#logging
rich